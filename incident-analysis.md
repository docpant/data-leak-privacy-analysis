# Incident Analysis

The data leak occurred because a manager shared a folder containing confidential internal documents with an employee and failed to remove access afterward. During a sales meeting, the employee accidentally shared the entire folder with an external business partner instead of sharing only specific marketing materials.

The incident demonstrates poor implementation of least privilege and insufficient access control management practices.
